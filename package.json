{
  "name": "@kitty-crow/profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "render": "node dist/profile-art.mjs",
    "validate": "npm run check && npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
